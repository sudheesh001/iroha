syntax = "proto3";

package iroha.protocol;

message Ping {
    string message = 1;
}

message Pong {
    string message = 1;
}

service LoggerLivingConfirmationService {
    rpc check (Ping) returns (Pong);
}
